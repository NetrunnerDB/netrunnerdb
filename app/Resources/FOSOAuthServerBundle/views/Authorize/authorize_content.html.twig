<div class="panel panel-default">
<div class="panel-heading">
<h3 class="panel-title" style="line-height:2em"><img src="/apple-touch-icon.png" style="height:2em"> NetrunnerDB Authorization</h3>
</div>
<div class="panel-body">
  <p>
   The application <em>{{ client.name }}</em> would like to connect to your account, <em>{{ app.user.username }}</em>.
  </p>
  <p>
  If you want to authorize a different account than <em>{{ app.user.username }}</em>, please <a href="{{ path("fos_user_security_logout") }}?redirect_to={{ app.request.requesturi | url_encode }}">log out and log back in</a>.
  </p>
  <p>
    If you agree, <em>{{ client.name }}</em> will be able to perform the following operations on your behalf:
  </p>
  <ul>
    <li>Read your decks saved on NetrunnerDB</li>
    <li>Modify a deck or create a new one on NetrunnerDB</li>
  </ul>
</p>
  <div class="form">
  {{ form_start(form) }}
    <input type="submit" class="btn btn-primary" name="accepted" value="{{ 'authorize.accept'|trans({}, 'FOSOAuthServerBundle') }}" />
    <input type="submit" class="btn btn-danger" name="rejected" value="{{ 'authorize.reject'|trans({}, 'FOSOAuthServerBundle') }}" />
    {{ form_row(form.client_id) }}
    {{ form_row(form.response_type) }}
    {{ form_row(form.redirect_uri) }}
    {{ form_row(form.state) }}
    {{ form_row(form.scope) }}
    {{ form_rest(form) }}
  {{ form_end(form) }}
  </div>
</div>
</div>
</div>
